<% semantic_form_for @user_session, :url => login_url do |f| %>
  	<%= f.input :email %>
  	<%= f.input :password, :as => :password %>
	<% f.buttons do %>
		<%= f.commit_button I18n.t(:'formtastic.actions.user_session.new') %>
	<% end %>
<% end %>